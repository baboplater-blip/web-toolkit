# goal.md — web-toolkit 목표

> 이 문서는 web-toolkit 작업의 **최종 목표와 성공 정의(Definition of Done)** 를 담는다.
> "무엇을 위해" 작업하는지 흔들릴 때 항상 이 문서로 돌아온다.
> 변경하려면 사용자 승인을 받는다. (규칙 자체는 [rules.md](rules.md) 참조)

---

## 1. 미션

**브라우저 안에서 완결되는 도구 모음 사이트.**
사용자 파일은 서버로 전송되지 않는다(browser-only). 모든 무거운 처리는
Web Worker + WASM 으로 로컬에서 수행한다. 회원가입·계정 없이 누구나 즉시 쓴다.

- 경로: `c:\Users\molma\Desktop\하네스\web-toolkit\`
- repo: `baboplater-blip/web-toolkit` (브랜치 `master`)
- 배포: Next.js 정적 export(`output: 'export'`) → Vercel 프로덕션

## 2. 핵심 성공 지표 (이 값들이 유지/향상되어야 한다)

| 지표 | 목표 상태 |
|---|---|
| 도구 수 | 지속 확장 (현재 **415종**, status.md 참조) |
| 다국어 카탈로그 | **ko·en·ja·zh 4개국어 완전 파리티** (EN=JA=ZH 키셋 동일) |
| 표면 동등성 | 모든 신규 도구가 기존 도구와 **카탈로그·맞춤가이드·발견성(클러스터·유스케이스·비교)** 전 표면에서 동등 |
| 빌드 게이트 | tsc 0 · eslint(신규 게이팅) 0 · build 성공 · budget 통과 · audit 전수 통과 · playwright 전수 통과 |
| Lighthouse | Perf 89+ / A11y 100 / BP 100 / SEO 95+ / CLS 0 |
| 하이드레이션 | 정적 export 페이지에서 SSR↔클라이언트 불일치 0 (pageerror 0) |
| 보안 원칙 | browser-only 위반 0 (서버 전송·API Route·Node 모듈 0) |

## 3. 성공 정의 (Definition of Done) — 한 라운드 단위

한 라운드(기능 추가/개선)는 아래를 **모두** 만족할 때만 "완료"다.

1. 구현이 [rules.md](rules.md)의 절대 규칙을 한 건도 위반하지 않는다.
2. [test.md](test.md)의 전체 게이트가 초록불이다(tsc→eslint→build→budget→audit→playwright).
3. 신규/변경 도구가 4개국어 표면 파리티를 갖추거나, **미완 표면이 status.md에 명시적으로 후속 항목으로 기록**된다.
4. `origin/master` 에 커밋·푸시되고 로컬↔원격이 동기화된다.
5. CLAUDE.md 변경이력 + 사용자 메모리 + [status.md](status.md) 가 갱신된다.
6. 사용자에게 **한국어 완료 요약 + 묶음형 다음 후보(추천 N번 포함)** 를 제시한다.

> 위 중 하나라도 빠지면 "거의 다 됐다"가 아니라 **미완**이다. test.md가 최종 심판이다.

## 4. 장기 방향

- 신규 도구 팩을 지속 확장하되, 매 팩은 **ko·en 생성 → ja·zh 파리티 → 맞춤가이드 → 발견성** 4단계 사이클로 기존 자산과 동등화한다.
- SEO 허브(가이드 3층·유스케이스·비교) 품질을 자동생성 문장에서 구현 기반 native 콘텐츠로 끌어올린다.
- 성능 예산을 지키며 번들을 가볍게 유지(트리거 지연 로드·검색 인덱스 분리 패턴).
- 도구 순수 로직은 `lib/tools/*` 로 추출해 단위 테스트 가능하게 한다.

## 5. 비목표 (하지 않는 것)

- 서버/백엔드 기능, 계정·로그인, 사용자 데이터 수집 → **하지 않는다.**
- 반응형 웹앱/대시보드형 UI 재설계(이 프로젝트는 고정 도구 사이트) → 범위 밖.
- 옛 채팅·에이전트 시스템 복원 → 이미 `_legacy/` 삭제, 복원 안 함.
